export type StorePubilc = {
    _id: string,
    name: string,
    logoUrl?: string,
    coverUrl?: string,
    slug?: string,
    description?: string,
    phone?: string,
    // productCategory?: string,
}